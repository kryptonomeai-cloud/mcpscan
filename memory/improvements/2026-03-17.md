# Improvement Proposal — 2026-03-17

## Problem
**iCloud backup cleanup has failed 3 consecutive days** (Mar 16 03:19, Mar 16 04:00, Mar 17 04:00). Each time, `find`/`ls` hangs on the iCloud path and cleanup is silently skipped. Stale backups are accumulating with zero alerting. Yesterday's proposal (#2026-03-16) covers the fix perfectly but remains 🔲 Proposed — this is now the 7th consecutive unapproved proposal.

**Meta-problem**: The improvement pipeline has zero throughput. 7 proposals, 0 implemented. The process generates ideas but nothing ships.

## Proposed Fix
**Escalate yesterday's iCloud fix (#2026-03-16) to auto-implement** — it's the lowest-risk, highest-evidence proposal in the queue:
1. Implement the iCloud health pre-check + timeout wrapper from yesterday's proposal
2. Create `~/.openclaw/workspace/backups/` as local fallback
3. Add explicit warning logs when iCloud is unreachable
4. Mark #2026-03-16 as ✅ Done

This is safe because: it only adds defensive `timeout` wrappers and a fallback path. No destructive changes. The existing backup still runs; this just prevents silent failures.

## Impact
- **Immediate**: Stops the recurring silent cleanup failure (3 days running)
- **Process**: Proves the improvement pipeline can actually ship
- **Tokens saved**: Stops re-analysing the same iCloud issue every morning

## Risk
- Very low. Adds only defensive wrappers. Worst case: backup goes to local disk instead of iCloud (still recoverable).

## Implementation
1. Find the paperless backup cron job script
2. Add `timeout 5 ls` health check before iCloud ops
3. Add `timeout 30` around `cp` and `find` to iCloud
4. Add local fallback path `~/.openclaw/workspace/backups/`
5. Update yesterday's proposal status

## Status: 🔲 Proposed
