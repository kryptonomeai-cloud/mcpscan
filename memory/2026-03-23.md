
## 04:00 - Paperless-NGX Backup
- Export ran successfully: 3 documents exported, 1.5M zip
- Copied to iCloud: ~/Library/.../MiniClaw-Backup/paperless-export-20260323.zip
- Cleanup of exports >7 days old triggered (iCloud folder can be slow to respond)
- NAS export path: /DATA/AppData/big-bear-paperless-ngx/paperless/export/

## Cron Job Audit & Fix (18:00 GMT)

**Problem:** 26 cron jobs running on MiniClaw — many referenced non-existent scripts and macOS-specific paths (`/Users/miniclaw/...`, `security find-generic-password`, etc.)

**Scripts Created:**
- `crypto-alert.sh` — CoinGecko price check, alerts on ≥5% moves
- `crypto-history-log.sh` — daily price logger to CSV
- `financial-summary.sh` — crypto + market data for briefings
- `git-sync.sh` — auto-commit and push workspace to GitHub
- `jarvis-tts.py` — ElevenLabs TTS for morning briefing voice

**Cron Jobs Updated (all paths → `~` relative):**
- crypto-price-alert, crypto-history-log, git-sync, paperless backup, openclaw backup, morning briefing, nightly LLM research, ideas lab, tools research, security sweep, process audit, weekly system audit, GPU maintenance, market summary, OpenClaw monitor

**Key Changes:**
- Removed all `/Users/miniclaw/` absolute paths → `~/`
- Removed `security find-generic-password` → hardcoded NAS password or env vars
- Removed Finnhub dependency (no API key) → web_search fallback for market data
- Removed ansible dependency → direct SSH commands for GPU maintenance
- Backup job now falls back to NAS if GPU unreachable
- Downgraded backup/GPU maintenance from Opus → Sonnet (cost saving)

**Still disabled (5):** MOSS Planner/Executor/Reviewer/Patrol (need API key), Paperless Auto-Classify

**Synced all scripts to dell3 via rsync**
